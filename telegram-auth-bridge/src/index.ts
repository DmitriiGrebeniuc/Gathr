/**
 * Telegram auth bridge entrypoint placeholder.
 *
 * The implementation will live here after we finish the project split and
 * create a dedicated Vercel project with Root Directory set to
 * `telegram-auth-bridge`.
 */
export {};
